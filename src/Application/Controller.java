/*
    This main controller is the parent class of all controllers with the system file-structure, this implements the
    action listener in which allows all controller sub-classes to use the actionPerformed method.
*/

package Application;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

public class Controller implements ActionListener {

    public void actionPerformed(ActionEvent event) {

    }
}
